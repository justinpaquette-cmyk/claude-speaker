
## Spoken Summary (TTS)

A Stop hook speaks responses aloud via macOS `say`. End every substantive response with a final line `🔊 <one short spoken sentence>` — plain conversational English, no markdown, no paths, no code. The hook speaks only that line; without it, it reads a sanitized 600-char fallback. Skip the line for trivial one-word replies.
